// "use strict";
// 
// window.onload = function(){
//   h.for_each(document.querySelectorAll("section"), function(el){
//     el.innerHTML = MD.replaceElement(el);
//   });
//   h.for_each(document.querySelectorAll("[data-code-cols]"), function(el){
//     var indexes = el.getAttribute("data-code-cols").split(/ +/g);
//     h.for_each(el.querySelectorAll("tr"), function(row){
//       h.for_each(row.children, function(td, index){
//         if(indexes.includes(index.toString())){
//           td.setAttribute("data-code", true);
//         }
//       });
//     });
//   });
// }
